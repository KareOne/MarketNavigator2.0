# Remote Worker Environment Configuration

# Main server orchestrator URL (WebSocket)
ORCHESTRATOR_URL=ws://YOUR_MAIN_SERVER_IP:8010/ws/worker

# Worker authentication token (get from admin)
WORKER_TOKEN=your-secure-token

# Type of API this worker runs
# Options: crunchbase, tracxn, social
API_TYPE=crunchbase

# Optional: Worker name for identification in logs
WORKER_NAME=cb-worker-1

# Local API URL (usually doesn't need to change)
LOCAL_API_URL=http://crunchbase_api:8003

# Connection settings
HEARTBEAT_INTERVAL=10
RECONNECT_DELAY=5
LOG_LEVEL=INFO
